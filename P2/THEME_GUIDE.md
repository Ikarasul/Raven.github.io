# 🎨 Theme Customization Guide

## วิธีปรับแต่ง Theme

### 📁 ไฟล์หลัก: `theme.js`

ไฟล์นี้เก็บการตั้งค่าทั้งหมดของ Theme ไว้ในที่เดียว แก้ได้ง่าย!

---

## 🎨 1. การปรับสี (Colors)

### เปลี่ยนสีหลัก (Primary)
```javascript
colors: {
    primary: '#34d399',      // ← แก้ที่นี่!
    primaryDark: '#10b981',
    primaryLight: '#6ee7b7',
}
```

**ตัวอย่างสีอื่นๆ:**
- 💙 Blue: `#3b82f6`
- 💜 Purple: `#a855f7`
- 🩷 Pink: `#ec4899`
- 🧡 Orange: `#f97316`

### เปลี่ยนสีพื้นหลัง
```javascript
bgLight: '#f0fdf4',  // สีพื้นหน้าเว็บ
bgWhite: '#ffffff',  // สีการ์ด
```

### เปลี่ยนสีตัวอักษร
```javascript
textPrimary: '#0f172a',    // ตัวอักษรหัวข้อ
textSecondary: '#64748b',  // ตัวอักษรรอง
textLight: '#94a3b8',      // ตัวอักษรอ่อน
```

---

## 📏 2. การปรับขนาดตัวอักษร

```javascript
fontSize: {
    xs: '0.75rem',      // เล็ก
    sm: '0.875rem',     // เล็กกว่าปกติ
    base: '1rem',       // ปกติ ← แก้ได้!
    lg: '1.125rem',     // ใหญ่
    xl: '1.25rem',      // ใหญ่มาก
}
```

**ทำให้ตัวอักษรทั้งหมดใหญ่ขึ้น:**
- เปลี่ยน `base: '1rem'` → `base: '1.125rem'`

---

## 📐 3. การปรับระยะห่าง (Spacing)

```javascript
spacing: {
    xs: '0.25rem',    // 4px
    sm: '0.5rem',     // 8px
    md: '1rem',       // 16px ← ระยะห่างปกติ
    lg: '1.5rem',     // 24px
    xl: '2rem',       // 32px
}
```

**ทำให้ทุกอย่างห่างกันมากขึ้น:**
- เปลี่ยน `md: '1rem'` → `md: '1.5rem'`

---

## 🔘 4. การปรับมุมโค้ง (Border Radius)

```javascript
borderRadius: {
    sm: '0.5rem',     // 8px - โค้งนิดหน่อย
    md: '1rem',       // 16px - โค้งปกติ
    lg: '1.5rem',     // 24px - โค้งมาก
    xl: '2rem',       // 32px - โค้งมากๆ
    full: '9999px',   // กลมเต็ม
}
```

**ทำให้ทุกอย่างโค้งมากขึ้น:**
- เปลี่ยนทุกค่าเป็น `2rem`, `3rem`, etc.

---

## 🎭 5. การปรับเงา (Shadows)

```javascript
shadows: {
    sm: '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
    md: '0 4px 6px -1px rgba(0, 0, 0, 0.1)',
    lg: '0 10px 15px -3px rgba(0, 0, 0, 0.1)',
    xl: '0 20px 25px -5px rgba(0, 0, 0, 0.1)',
    glow: '0 0 20px rgba(52, 211, 153, 0.4)',  // เงาเรืองแสง
}
```

**เพิ่มความเข้มเงา:**
- เปลี่ยนค่าสุดท้าย `0.1` → `0.3`

---

## ⚡ 6. ความเร็ว Animation

```javascript
transitions: {
    fast: '150ms',     // เร็ว
    normal: '300ms',   // ปกติ
    slow: '500ms',     // ช้า
}
```

**ทำให้ Animation ช้าลง (นุ่มนวลขึ้น):**
- เปลี่ยน `normal: '300ms'` → `normal: '500ms'`

---

## 📱 7. การจัดวาง Layout

```javascript
layout: {
    navHeight: '80px',           // ความสูง Navigation
    containerMaxWidth: '1200px', // ความกว้างสูงสุด
    cardPadding: '1.5rem',       // Padding ในการ์ด
    cardGap: '1.5rem',           // ช่องว่างระหว่างการ์ด
}
```

**ทำให้การ์ดกว้างขึ้น:**
- เปลี่ยน `containerMaxWidth: '1200px'` → `'1400px'`

**เพิ่มระยะห่างในการ์ด:**
- เปลี่ยน `cardPadding: '1.5rem'` → `'2rem'`

---

## 🌈 ตัวอย่าง Theme สำเร็จรูป

### 💙 Blue Ocean Theme
```javascript
colors: {
    primary: '#3b82f6',      // Blue
    primaryDark: '#1d4ed8',
    primaryLight: '#60a5fa',
    bgLight: '#eff6ff',      // Blue tint
}
```

### 💜 Purple Dream Theme
```javascript
colors: {
    primary: '#a855f7',      // Purple
    primaryDark: '#7c3aed',
    primaryLight: '#c084fc',
    bgLight: '#faf5ff',      // Purple tint
}
```

### 🩷 Pink Candy Theme
```javascript
colors: {
    primary: '#ec4899',      // Pink
    primaryDark: '#db2777',
    primaryLight: '#f472b6',
    bgLight: '#fdf2f8',      // Pink tint
}
```

---

## 📝 วิธีใช้งาน

1. **เปิดไฟล์**: `theme.js`
2. **แก้ค่าที่ต้องการ**: เช่น `primary: '#3b82f6'`
3. **บันทึก**: Ctrl+S
4. **รีเฟรช**: F5

**แค่นี้เอง!** ✨

---

## 🎯 Tips & Tricks

### ทำให้ Theme นุ่มนวล
- เพิ่มค่า `transitions.normal` เป็น `500ms`
- เพิ่มค่า `effects.hoverLift` เป็น `-6px`

### ทำให้ Theme กระชับ
- ลดค่า `spacing.md` เป็น `0.75rem`
- ลดค่า `cardPadding` เป็น `1rem`

### ทำให้ Theme หรูหรา
- เพิ่มค่า shadow ทุกตัว
- ใช้ `borderRadius` ใหญ่ขึ้น
- เปลี่ยนเป็นสีทอง: `primary: '#f59e0b'`

---

## 🆘 ปัญหาที่พบบ่อย

**Q: แก้แล้วไม่เปลี่ยน?**
- A: ลองกด Ctrl+Shift+R (Hard Refresh)

**Q: สีไม่สวย?**
- A: ใช้ [Coolors.co](https://coolors.co) หาสีที่เข้ากัน

**Q: อยากกลับค่าเดิม?**
- A: Theme เดิมคือ "Cute & Fresh Mint" ตามด้านบน

---

สนุกกับการปรับแต่ง! 🎨✨
